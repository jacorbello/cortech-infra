# TEMPLATE - Do not commit actual credentials
# Copy to secret.yaml and fill in values, then apply manually:
#   kubectl apply -f secret.yaml
#
# Or create directly:
#   kubectl create secret generic proxmox-exporter-credentials \
#     --namespace=observability \
#     --from-literal=user="pve-exporter@pve" \
#     --from-literal=token_name="prometheus" \
#     --from-literal=token_value="<TOKEN_VALUE>"
apiVersion: v1
kind: Secret
metadata:
  name: proxmox-exporter-credentials
  namespace: observability
type: Opaque
stringData:
  user: "pve-exporter@pve"
  token_name: "prometheus"
  token_value: "<TOKEN_VALUE_FROM_PVEUM>"
