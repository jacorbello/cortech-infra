apiVersion: v1
kind: Service
metadata:
  name: snmp-exporter
  namespace: observability
  labels:
    app.kubernetes.io/name: snmp-exporter
    app.kubernetes.io/component: exporter
    release: prometheus
spec:
  type: ClusterIP
  ports:
  - name: metrics
    port: 9116
    targetPort: metrics
    protocol: TCP
  selector:
    app.kubernetes.io/name: snmp-exporter
