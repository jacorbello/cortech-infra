apiVersion: v1
kind: Service
metadata:
  name: postiz-redis
  namespace: plotlens-marketing
  labels:
    app: postiz-redis
    app.kubernetes.io/part-of: postiz
spec:
  type: ClusterIP
  ports:
    - name: redis
      port: 6379
      targetPort: redis
  selector:
    app: postiz-redis
