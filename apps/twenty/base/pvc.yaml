apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: twenty-data
  namespace: crm
spec:
  # RWX is required: server and worker both mount this share.
  accessModes: [ReadWriteMany]
  storageClassName: nfs-node3
  resources:
    requests:
      storage: 20Gi
