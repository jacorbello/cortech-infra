apiVersion: apps/v1
kind: Deployment
metadata:
  name: osint-worker
  namespace: osint
spec:
  template:
    spec:
      containers:
        - name: osint-worker
          resources:
            requests:
              cpu: 250m
              memory: 2Gi
            limits:
              cpu: "1"
              memory: 5Gi
