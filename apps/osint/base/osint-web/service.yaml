apiVersion: v1
kind: Service
metadata:
  name: osint-web
  namespace: osint
  labels:
    app: osint-web
    app.kubernetes.io/part-of: osint-platform
spec:
  type: ClusterIP
  selector:
    app: osint-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
