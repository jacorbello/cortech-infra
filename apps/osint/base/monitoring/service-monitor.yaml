apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: osint-core
  namespace: osint
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: osint-core
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
