apiVersion: v1
kind: Service
metadata:
  name: osint-core
  namespace: osint
  labels:
    app: osint-core
    app.kubernetes.io/part-of: osint-platform
spec:
  type: ClusterIP
  selector:
    app: osint-core
  ports:
    - name: http
      port: 8000
      targetPort: http
      protocol: TCP
