apiVersion: v1
kind: Service
metadata:
  name: vllm-inference
  namespace: inference
  labels:
    app: vllm
    app.kubernetes.io/part-of: inference-platform
spec:
  type: NodePort
  selector:
    app: vllm
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30850
      protocol: TCP
      name: http
